# r2/r5 高污染率扩展统计说明（中文）

- 该扩展仅覆盖真实评论数最高的 10 门课程，是离线高污染率压力测试，不估计平台真实发生率。
- 所有配对差值都相对既有 Phase 2 已持久化的同课程、同 pipeline clean baseline；不会用新的调用覆盖冻结基线。
- 先在同一课程内平均三个生成种子，再以课程为单位进行 5,000 次非参数 bootstrap，避免把 seed 当作独立课程样本。
- strict subset 是描述性敏感性切片；样本更小，不单独作强结论。人工盲审未完成前，结果均为自动化评估。
