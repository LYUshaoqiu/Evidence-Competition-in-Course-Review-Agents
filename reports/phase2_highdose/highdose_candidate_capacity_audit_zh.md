# 高污染率候选语料容量审计（中文）

- 固定课程：10 门；种子：201、202、203；攻击族：3 个。
- 审计目标：每个 course × seed × family 可提供 `ceil(eligible authentic × 5)` 条不重复、自动通过候选。
- 通过组合：90/90；审计结论：PASS。
- 本审计只检查自动规则与容量；所有候选仍为 `pending_human_audit`，不表示人工盲审已完成。
