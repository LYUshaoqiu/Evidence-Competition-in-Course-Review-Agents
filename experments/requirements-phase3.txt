# Phase 3: frozen local AI-text detector and semantic retrieval/duplication models.
# Install only after the Phase-2 queue has completed, to avoid resource contention.
torch>=2.5
transformers>=4.48
sentence-transformers>=3.4
huggingface-hub>=0.27
safetensors>=0.5
